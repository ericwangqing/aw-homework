+component('book')
  +list(['ISBN', '书名', '作者', '出版社', '出版时间', '价格', '库存'])
  +detail()
    +fieldset('书籍信息')
      +row()
        +no-blank-text('ISBN')
        +no-blank-text('书名')(span=HALF_ROW)
        +no-blank-text('作者')
      +row()
        +datetime('出版时间')
        +no-blank-text('出版社')
        +number('价格')
        +number('库存')
      +row()
        +block-text('内容简介')(span=FULL_ROW)
